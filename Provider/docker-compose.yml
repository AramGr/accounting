version: "3.9"

services:
  php:
    build:
      context: ./docker/php
    container_name: provider-test-php
    working_dir: /var/www/html
    volumes:
      - ./app:/var/www/html
    depends_on:
      - db

  nginx:
    image: nginx:stable
    container_name: provider-test-nginx
    ports:
      - "8080:80"
    volumes:
      - ./app/public:/var/www/html/public
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php

  db:
    image: mysql:8.4
    container_name: provider-test-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: provider
      MYSQL_USER: admin
      MYSQL_PASSWORD: adminProviderPass
    ports:
      - "3310:3306"
    volumes:
      - db-data:/var/lib/mysql

volumes:
  db-data:
